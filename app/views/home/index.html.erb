<h1>Home</h1>
<% @location.each do |l| %>
  <ul>
    <%= link_to l.location, location_path(l.id) %>
    <br><%= l.blurb %>
    <br><%= l.fb %>
    <br><%= l.meetup %>
    <br><%= l.geo %>
  </ul>
<% end %>
